<#setting locale=locale>
<#if fiatPriceProvider??>
<#assign tick = fiatPriceProvider.getLocalCurValue(payload.balance,getTo().getPhoneNumber())!"error">
</#if>
<#if tick?? & tick != "error">
<#assign fiat = " ("+tick.getLastFactored()?string("#,##0.##")+tick.getCurCode()+")">
<#else>
<#assign fiat = "">
</#if>
${resBundle("Balance",payload.miliBalance?string("#,##0.#####"),fiat)}